
<li id="property_<%= property.id %>">

  <div class="strong"><%= link_to "#{property.name}", property.name %></div>
  <%= link_to "(delete)", property, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
  <br>
  <br>

</li>
